# AWS Deploy Configuration
# Copy this file to deploy.env and fill in your values.
# deploy.env is gitignored — do not commit real values.

AWS_REGION=ap-southeast-2
PROCESSOR_FUNCTION_NAME=your-email-processor-function-name
API_FUNCTION_NAME=your-email-api-function-name
SPA_BUCKET=your-spa-s3-bucket-name
CLOUDFRONT_DISTRIBUTION_ID=   # optional — leave blank if not using CloudFront
